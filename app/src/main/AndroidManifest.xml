<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.techkis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.forum.EditForumActivity"
            android:parentActivityName=".ui.forum.DetailForumActivity" />
        <activity
            android:name=".ui.forum.DetailForumActivity"
            android:parentActivityName=".ui.ForumActivity" />
        <activity
            android:name=".ui.forum.AddForumActivity"
            android:parentActivityName=".ui.ForumActivity" />
        <activity android:name=".ui.ForumActivity" />
        <activity android:name=".ui.users.ProfileActivity" />
        <activity
            android:name=".ui.admin.AddNewsActivity"
            android:parentActivityName=".ui.HomeActivity" />
        <activity
            android:name=".ui.news.NewsViewActivity"
            android:parentActivityName=".ui.HomeActivity" />
        <activity android:name=".ui.HomeActivity">
        </activity>
        <activity android:name=".ui.users.RegisterActivity" />
        <activity android:name=".ui.users.LoginActivity" />
    </application>

</manifest>